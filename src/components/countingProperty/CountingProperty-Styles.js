import makeStyles from "@mui/styles/makeStyles";

const useStyles = makeStyles((theme) => ({
  card: {
   
    "& .MuiCard-root":{
    width: "142px",
    height: "112px",
    },
  },
}));

export { useStyles };
